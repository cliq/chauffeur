// Shared defaults. Keep machine-specific signing settings in LocalSigning.xcconfig.
DEVELOPMENT_TEAM =
CODE_SIGN_STYLE = Automatic
CODE_SIGN_IDENTITY = Apple Development

APP_BUNDLE_ID = dev.cliq.chauffeur

#include? "LocalSigning.xcconfig"
